# Query Records

Use this folder to document submitted query points if sharing is permitted by the programme rules.

Recommended files:

- `submitted_queries.csv` for final submitted query points.
- `query_rationale.md` for short notes explaining exploration versus exploitation choices.

Keep the canonical modelling dataset in `data/raw/bbo_query_history.csv` so the notebook has one stable input path.
